# TEMPLATE — copy this to secrets.enc.yaml, fill in real values, encrypt it,
# then this plaintext copy should not be the one that sticks around. See
# SECRETS.md for the full sops + age walkthrough. Short version:
#
#   sudo apt install age              # sops itself needs a manual .deb, see SECRETS.md
#   age-keygen -o ~/.config/sops/age/keys.txt
#   # note the "Public key: age1..." line it prints
#
#   cp secrets.example.yaml secrets.enc.yaml
#   nano secrets.enc.yaml             # fill in real values below
#   sops --encrypt --age <your-age1-public-key> -i secrets.enc.yaml
#
# From then on secrets.enc.yaml is ciphertext (values only — keys stay
# readable) and is meant to be committed to git.
#
# Use it with any script via the wrapper:
#   ./run-with-sops.sh python3 02_nornir_scale.py
NORNIR_USERNAME: admin
NORNIR_PASSWORD: admin
